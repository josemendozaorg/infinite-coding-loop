Define the project structure for the following specification.
Spec: {{source_content}}

Produce a JSON object with directory mapping.
Example:
```json
{
  "src": ["main.rs", "lib.rs"],
  "tests": ["integration.rs"]
}
```
Output ONLY the JSON.
