{
    "$id": "https://infinite-coding-loop.dass/schemas/meta/base.schema.json",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "Meta-Ontology Base Primitives",
    "description": "Foundational definitions for any ontology compatible with the DASS engine.",
    "type": "object",
    "$defs": {
        "EntityMetadata": {
            "type": "object",
            "title": "Meta-Entity",
            "description": "A node in the graph, identified by its name.",
            "required": [
                "name"
            ],
            "properties": {
                "name": {
                    "type": "string",
                    "description": "The unique name or kind identifier for this entity."
                },
                "type": {
                    "type": "string",
                    "enum": [
                        "Agent",
                        "Document",
                        "Code",
                        "Other"
                    ],
                    "default": "Other",
                    "description": "The functional classification of this entity."
                },
                "modelType": {
                    "type": "string",
                    "enum": [
                        "High Reasoning",
                        "Fast Execution",
                        "Daily Driver"
                    ],
                    "description": "Classification of the node according to intelligence/execution-speed tiers."
                },
                "model": {
                    "type": "string",
                    "description": "Specific AI model override for this node."
                },
                "aiCli": {
                    "type": "string",
                    "description": "Specific AI CLI override for this node."
                }
            }
        },
        "MetaVerb": {
            "type": "object",
            "title": "Meta-Verb",
            "description": "An action or relationship type in the graph.",
            "required": [
                "name"
            ],
            "properties": {
                "name": {
                    "type": "string",
                    "description": "The name of the relationship type (e.g. 'creates')."
                }
            }
        },
        "MetaRelationship": {
            "type": "object",
            "required": [
                "source",
                "target",
                "type"
            ],
            "properties": {
                "source": {
                    "$ref": "#/$defs/EntityMetadata"
                },
                "target": {
                    "$ref": "#/$defs/EntityMetadata"
                },
                "type": {
                    "$ref": "#/$defs/MetaVerb"
                }
            }
        }
    }
}